version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: temporary
    ports:
      - "5433:5432"
    command: postgres -c max_prepared_transactions=100
